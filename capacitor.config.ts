import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'mediasession',
  webDir: 'dist/mediasession',
  bundledWebRuntime: false
};

export default config;
