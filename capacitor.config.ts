import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'capacitormediasession',
  webDir: 'dist/capacitormediasession',
  bundledWebRuntime: false
};

export default config;
